package org.pispeb.treff_client.data.networking.commands;

/**
 * Abstract Response
 */

public abstract class AbstractResponse {
    // No common methods. Still, all responses need to share a common superclass
}
