package org.pispeb.treff_client.home.groupList;

import android.support.v7.widget.RecyclerView.ViewHolder;
import android.view.View;

/**
 * Holding Layout for single item of the FriendList.
 * Passing on onCLick-events on that item to {@link GroupListFragment}
 */

public class GroupListViewHolder extends ViewHolder {
    public GroupListViewHolder(View itemView) {
        super(itemView);
    }
}
